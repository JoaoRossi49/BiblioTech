@model IEnumerable<BiblioTech.Models.Livro>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeLivro)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGenero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QuantidadePaginas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdAutor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeLivro)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdGenero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QuantidadePaginas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdAutor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLivo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLivo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLivo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
